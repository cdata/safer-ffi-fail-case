[package]
name = "tmp"
version = "0.1.0"
edition = "2021"

[features]
headers = []
# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow = "^1"
safer-ffi = { version = "0.1.2", features = ["proc_macros", "headers"] }